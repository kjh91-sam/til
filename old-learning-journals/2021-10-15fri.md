**Don't forget! : javascript things**
- ~~`includes` for array, `contains` for string~~  
    **It was the opposite!** `includes` for string, `contains` for array!
- `for of` for array, `for in` for object